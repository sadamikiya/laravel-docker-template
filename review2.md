# Laravel Lesson レビュー②

## Todo編集機能

### @method('PUT')を記述した行に何が出力されているか
- デベロッパーツールでHTMLを確認すると。<input type="hidden" name="_method" value="PUT">が出力される。_methodの値PUTをチェックしている。

### findメソッドの引数に指定しているIDは何のIDか
- findメソッドの引数に指定しているIDはデータベースのレコードを識別するためのidカラムの値のことです。

### findメソッドで実行しているSQLは何か
- select * from `todos` where `id` = ? limit 1　でありこのクエリは指定されたidに一致するレコードをtodosテーブルから1件取得するためのもの。

### findメソッドで取得できる値は何か
- findメソッドで取得できる値はデータベースにある指定したidの該当するモデルのインスタンスであり、レコードのカラム値にアクセスできる。

### saveメソッドは何を基準にINSERTとUPDATEを切り替えているのか
- INSERTの場合には新規作成の場合。
- UPDATEの場合には既存のデータがある場合。

## Todo論理削除

### traitとclassの違いとは
- traitでは１つのクラスに複数のtraitを追加できる。
- trait自体はクラスと違いインスタンスできない。
- classは他のクラスを1つだけ継承。
- classはnewを使うことでインスタンス化可能

### traitを使用するメリットとは
- traitを使うことで、複数のクラス間でコードを共通化、再利用することが可能になる。これによりコードの重複を防ぐことができる。変更や修正も1箇所で済むようになる。

## その他

### TodoControllerクラスのコンストラクタはどのタイミングで実行されるか
- TodoControllerクラスのコントラクタはルートメソッドが実行され際、コントローラのインスタンスが生成される時に実行される。

### RequestクラスからFormRequestクラスに変更した理由
- FormRequestクラスはフォームデータのバリデーションを行うために使用する。
このクラスにバリデーションルールをまとめて記載することができバリデーションを別のクラスに分けて管理することができる。

### $errorsのhasメソッドの引数・返り値は何か
- $errors->has('入力欄のname属性')の引数としてバリデーションで確認したいname属性を指定している。
- has()の返り値はその入力欄でバリデーションエラーが発生しているか判定します。エラーがあればtrue,なければfalseを返します。

### $errorsのfirstメソッドの引数・返り値は何か
- $errors->first('入力欄のname属性')の引数としてバリデーションのエラーを取得したいname属性を指定している。
- first()の返し値は入力欄で最初に発生したエラーメッセージを返します。

### フレームワークとは何か
- 枠組みのことを意味しています。フレームワークを学ぶことによるメリットは容易に一定品質のプロダクトを作成することができる。枠組みがあるため初見のアプリケーションでもコードの記述場所が特定しやすいなどがある。

### MVCはどういったアーキテクチャか
- アプリケーションをModel,view,Controller,Routingの役割に分けることで、再利用性と可読性の恩恵を受けることができる。

### ORMとは何か、またLaravelが使用しているORMは何か
- クラスとデータベースのテーブルの関連付けをする仕組み。これをすることでSQLを直接記述することなくデータベースへの操作をプログラム内のクラスやそのメソッドを通じて行えるようになる。
- Laravelで使用されているORMはEloquentが使用されています。これはデータベースとテーブルで関連付けられたクラスがmodelとして定義されmodelを通じてデータベースのレコードを操作することができます。

### composer.json, composer.lockとは何か
- composer.jsonはプロジェクトがどのようなライブラリや設定を必要としているかを明示するための設定ファイルです。
- composer.lockは実際にインストールされた依存関係の正確なバージョンやメタデータを記録するファイルです。他の開発者はcomposer.lockを基にcomposer installを実行することで同じ環境を再現できる。

### composerでインストールしたパッケージ（ライブラリ）はどのディレクトリに格納されるのか
- プロジェクトのvendorディレクトリに格納されます。composerの依存関係を管理する場所として機能する。